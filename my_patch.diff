From 1e4b3549c22ba0384546251c004fde1c46753347 Mon Sep 17 00:00:00 2001
From: "google-labs-jules[bot]"
 <161369871+google-labs-jules[bot]@users.noreply.github.com>
Date: Fri, 13 Mar 2026 14:22:09 +0000
Subject: [PATCH] Draft: Intake template resolver partial repair, Appendix F
 disclosure, blocked checkpoint

---
 .../intake/fixture_inventory.md               | 12 +++
 .../intake/golden_artifact_verification.md    |  9 +++
 delivery_artifacts/intake/test_inventory.md   |  7 ++
 .../intake/traceability_matrix.csv            |  5 ++
 src/engine/intake/determination.js            | 13 ++-
 src/engine/intake/limitations_appendix_f.md   | 17 ++++
 src/engine/intake/template_resolver.js        | 52 ++++++++++++
 .../intake/templates/ELIGIBLE_DESKTOP.md      |  9 +--
 .../templates/ELIGIBLE_DESKTOP_MOBILE.md      |  9 +--
 src/engine/intake/templates/NOT_ELIGIBLE.md   |  4 +-
 .../templates/NOT_ELIGIBLE_CONSTRAINTS.md     |  8 +-
 tests/template_resolution.test.js             | 81 +++++++++++++++++++
 12 files changed, 206 insertions(+), 20 deletions(-)
 create mode 100644 delivery_artifacts/intake/fixture_inventory.md
 create mode 100644 delivery_artifacts/intake/golden_artifact_verification.md
 create mode 100644 delivery_artifacts/intake/test_inventory.md
 create mode 100644 delivery_artifacts/intake/traceability_matrix.csv
 create mode 100644 src/engine/intake/limitations_appendix_f.md
 create mode 100644 src/engine/intake/template_resolver.js
 create mode 100644 tests/template_resolution.test.js

diff --git a/delivery_artifacts/intake/fixture_inventory.md b/delivery_artifacts/intake/fixture_inventory.md
new file mode 100644
index 0000000..f7f0741
--- /dev/null
+++ b/delivery_artifacts/intake/fixture_inventory.md
@@ -0,0 +1,12 @@
+# Intake Fixture Inventory (Appendix D)
+
+| Fixture ID | Requirement | File Path | Status |
+|---|---|---|---|
+| FIX_INTAKE_T1 | Prove T1 template generation | `N/A - Unit Tested Only` | MISSING - E2E orchestrator unverified |
+| FIX_INTAKE_T2 | Prove T2 template generation | `N/A - Unit Tested Only` | MISSING - E2E orchestrator unverified |
+| FIX_INTAKE_T3 | Prove T3 generation | `N/A` | BLOCKED - See Appendix F |
+| FIX_INTAKE_T4 | Prove T4 template generation | `N/A - Unit Tested Only` | MISSING - E2E orchestrator unverified |
+| FIX_INTAKE_T5 | Prove T5 template generation | `N/A - Unit Tested Only` | MISSING - E2E orchestrator unverified |
+| FIX_INTAKE_T6 | Prove T6 template generation | `N/A - Unit Tested Only` | MISSING - E2E orchestrator unverified |
+
+*Completion Status is blocked because the required end-to-end fixtures do not exist.*
\ No newline at end of file
diff --git a/delivery_artifacts/intake/golden_artifact_verification.md b/delivery_artifacts/intake/golden_artifact_verification.md
new file mode 100644
index 0000000..c8f7ea0
--- /dev/null
+++ b/delivery_artifacts/intake/golden_artifact_verification.md
@@ -0,0 +1,9 @@
+# Golden Artifact Verification (Appendix E)
+
+| Artifact | Verification Source | Generation Mechanism | Result |
+|---|---|---|---|
+| `ELIGIBLE_DESKTOP_MOBILE.md` (T1) | `AFIntakeTemplate.pdf` | Hardcoded in repository template files, parsed via `template_resolver.js` | Verified |
+| `ELIGIBLE_DESKTOP.md` (T2) | `AFIntakeTemplate.pdf` | Hardcoded in repository template files, parsed via `template_resolver.js` | Verified |
+| `NOT_ELIGIBLE.md` (T4) | `AFIntakeTemplate.pdf` | Hardcoded in repository template files, parsed via `template_resolver.js` | Verified |
+| `NOT_ELIGIBLE_CONSTRAINTS.md` (T5/T6) | `AFIntakeTemplate.pdf` | Hardcoded in repository template files, parsed via `template_resolver.js` with regex placeholder injection | Verified |
+| `DETERMINATION.txt` | E2E Output | Generated by `orchestrator.js` | **MISSING** - Cannot verify output payload |
\ No newline at end of file
diff --git a/delivery_artifacts/intake/test_inventory.md b/delivery_artifacts/intake/test_inventory.md
new file mode 100644
index 0000000..db427ff
--- /dev/null
+++ b/delivery_artifacts/intake/test_inventory.md
@@ -0,0 +1,7 @@
+# Intake Test Inventory (Appendix E)
+
+| Test Suite | Purpose | Execution Type | Status |
+|---|---|---|---|
+| `tests/template_resolution.test.js` | Proves canonical templates resolve properly from enums, fail properly on null/undefined, and inject parameters safely. | Unit | PASS |
+| `tests/template_resolution.test.js` (T3) | Proves `T3_DESKTOP_MOBILE_CONSTRAINED` throws required doctrine gap error. | Unit | PASS |
+| Orchestrator E2E | Proves the resolver is invoked and writes `DETERMINATION.txt` to output payload. | E2E | MISSING |
\ No newline at end of file
diff --git a/delivery_artifacts/intake/traceability_matrix.csv b/delivery_artifacts/intake/traceability_matrix.csv
new file mode 100644
index 0000000..90e0e97
--- /dev/null
+++ b/delivery_artifacts/intake/traceability_matrix.csv
@@ -0,0 +1,5 @@
+doctrine_rule,code_location,function_or_mechanism,status,notes
+"AFIntakeTemplate.pdf provides canonical body text for only 4 templates","src/engine/intake/template_resolver.js","resolveTemplate()","IMPLEMENTED","Mappings restrict to exactly 4 source files."
+"AF 1-23 locks 6 permitted external intake determinations","engine/intake/enums.js","DETERMINATION_TEMPLATE","IMPLEMENTED","6 enums defined, mapping implemented."
+"Template 3 is missing canonical body text","src/engine/intake/template_resolver.js","resolveTemplate()","IMPLEMENTED","T3 resolution triggers hard throw."
+"Fixture D.3 required for Template 3","delivery_artifacts/intake/fixture_inventory.md","N/A","BLOCKED","Documented in Appendix F, blocking completion."
\ No newline at end of file
diff --git a/src/engine/intake/determination.js b/src/engine/intake/determination.js
index 71f2192..abd15a0 100644
--- a/src/engine/intake/determination.js
+++ b/src/engine/intake/determination.js
@@ -39,7 +39,7 @@ function pickConstraintsTemplate(runUnits) {
   const dt = ENUMS.DETERMINATION_TEMPLATE || {};

   if (isBot) {
-    return dt.T5_NOT_ELIGIBLE_CONSTRAINTS_BOTMITIGATION
+    return dt.T5_NOT_ELIGIBLE_CONSTRAINTS_BOT
         || dt.T5_NOT_ELIGIBLE_CONSTRAINTS
         || dt.T5_CONSTRAINTS;
   }
@@ -74,7 +74,16 @@ function computeDetermination(runUnits, mobileInScope) {
   if (anyConstrained) {
     const t = pickConstraintsTemplate(runUnits);
     if (!t) throw new Error("CANONICAL_ENUMS_MISSING_CONSTRAINTS_TEMPLATE");
-    return { category: t, note: null };
+
+    let constraintClass = null;
+    for (const ru of runUnits || []) {
+      if (ru && ru.outcome === OUTCOME_CONSTRAINED) {
+        constraintClass = getConstraintClass(ru);
+        break;
+      }
+    }
+
+    return { category: t, note: null, constraintClass };
   }

   return { category: ENUMS.DETERMINATION_TEMPLATE.T4_NOT_ELIGIBLE, note: null };
diff --git a/src/engine/intake/limitations_appendix_f.md b/src/engine/intake/limitations_appendix_f.md
new file mode 100644
index 0000000..ed49726
--- /dev/null
+++ b/src/engine/intake/limitations_appendix_f.md
@@ -0,0 +1,17 @@
+---
+limitation_id: LIMITATION-INTAKE-T3-GAP-001
+doctrine_section: AF 1-23 / Intake Outputs
+requirement_summary: AF 1-23 locks 6 permitted external intake determinations, including Template 3 (ELIGIBLE FOR DESKTOP TECHNICAL RECORD BUILD / MOBILE BASELINE: CONSTRAINED). Fixture D.3 is required for Template 3.
+current_state: The engine defines all 6 enums. However, AFIntakeTemplate.pdf only provides canonical body text for 4 templates (T1, T2, T4, T5/T6). The resolver cannot synthesize non-canonical text for T3.
+why_not_enforced: Synthesizing an unapproved template violates governance rules against improvising determination payloads.
+risk_if_unaddressed: A real intake run hitting the T3 outcome will lack external messaging output, causing a pipeline failure.
+temporary_controls: The resolveTemplate function explicitly hard-fails if T3 is requested, throwing a defined error.
+tests_missing_or_partial: End-to-end integration tests for T3 generation cannot be written until a template exists.
+fixtures_missing_or_partial: Fixture D.3 is missing and cannot be generated.
+expected_failure_mode: Hard fail on T3 resolution at execution time.
+blocks_completion: true
+planned_followup: Await canonical governed text for T3 to be added to AFIntakeTemplate.pdf.
+---
+
+# Appendix F: Limitations and Doctrine Gaps
+This file formally registers limitations against the AF 1-23 machine-bindable spec. The metadata above conforms to the mandatory Appendix F fields.
\ No newline at end of file
diff --git a/src/engine/intake/template_resolver.js b/src/engine/intake/template_resolver.js
new file mode 100644
index 0000000..3adc662
--- /dev/null
+++ b/src/engine/intake/template_resolver.js
@@ -0,0 +1,52 @@
+'use strict';
+
+const fs = require('fs');
+const path = require('path');
+const { ENUMS } = require('./locked.js');
+
+/**
+ * AFIntakeTemplate.pdf includes canonical text for 4 determination templates only.
+ * T3_DESKTOP_MOBILE_CONSTRAINED has no approved template text and must not be synthesized.
+ */
+function resolveTemplate(category, constraintClass = null, constraintBasis = null) {
+  if (category === undefined || category === null) {
+    throw new Error('Template resolution failed: category cannot be null or undefined.');
+  }
+
+  const dt = ENUMS.DETERMINATION_TEMPLATE || {};
+  const templatesDir = path.join(__dirname, 'templates');
+
+  if (category === dt.T3_DESKTOP_MOBILE_CONSTRAINED) {
+    throw new Error('Missing canonical determination template for T3_DESKTOP_MOBILE_CONSTRAINED. AFIntakeTemplate.pdf provides no approved text for this state. Refusing to synthesize non-canonical determination content.');
+  }
+
+  if (category === dt.T1_DUAL) {
+    return fs.readFileSync(path.join(templatesDir, 'ELIGIBLE_DESKTOP_MOBILE.md'), 'utf8');
+  }
+
+  if (category === dt.T2_DESKTOP) {
+    return fs.readFileSync(path.join(templatesDir, 'ELIGIBLE_DESKTOP.md'), 'utf8');
+  }
+
+  if (category === dt.T4_NOT_ELIGIBLE) {
+    return fs.readFileSync(path.join(templatesDir, 'NOT_ELIGIBLE.md'), 'utf8');
+  }
+
+  if (category === dt.T5_NOT_ELIGIBLE_CONSTRAINTS_BOT || category === dt.T6_NOT_ELIGIBLE_CONSTRAINTS_OTHER) {
+    if (!constraintClass) {
+      throw new Error('Template resolution failed: constraintClass is required for T5/T6 constraints template.');
+    }
+    if (!constraintBasis) {
+      throw new Error('Template resolution failed: constraintBasis is required for T5/T6 constraints template.');
+    }
+
+    let tpl = fs.readFileSync(path.join(templatesDir, 'NOT_ELIGIBLE_CONSTRAINTS.md'), 'utf8');
+    tpl = tpl.replace('{{CONSTRAINT_CLASS}}', constraintClass);
+    tpl = tpl.replace('{{CONSTRAINT_BASIS}}', constraintBasis);
+    return tpl;
+  }
+
+  throw new Error(`Unknown determination category for template resolution: ${category}`);
+}
+
+module.exports = { resolveTemplate };
\ No newline at end of file
diff --git a/src/engine/intake/templates/ELIGIBLE_DESKTOP.md b/src/engine/intake/templates/ELIGIBLE_DESKTOP.md
index ee5ad34..ff2595d 100644
--- a/src/engine/intake/templates/ELIGIBLE_DESKTOP.md
+++ b/src/engine/intake/templates/ELIGIBLE_DESKTOP.md
@@ -1,4 +1,4 @@
-﻿ACCESS FORENSICS
+ACCESS FORENSICS
 INTAKE DETERMINATION
 MATTER ID: [AF-YYYY-NNNN]

@@ -8,12 +8,9 @@ Based on Access Forensics intake assessment of the complaint or demand materials

 This determination reflects feasibility under a bounded intake gate and does not quantify the number of conditions present or absent.

-BASELINE EXECUTION CONTEXTS:
-Desktop baseline is executed at 1366 x 900 CSS pixels at 100% browser zoom. Browser version is recorded in the execution log. Reflow is evaluated separately per WCAG 1.4.10 using both direct and zoom-path methods.
-
 NEXT STEPS:
 Full engagement involves execution under a defined desktop browser context pursuant to our controlled capture protocols. All capture artifacts are logged, time-stamped, and sealed within a structured evidentiary packet utilizing industry-standard cryptographic integrity verification (SHA-256) and documented chain-of-custody controls.

-The evidentiary packet includes, as applicable to the claims asserted in the submitted materials: high-resolution screenshots, page source and DOM captures, rendered text extracts, network activity records including requests, responses, headers, and timing data, console and runtime logs, interaction and navigation logs, and environment metadata including viewport, user agent, device profile, and execution settings. Each artifact is indexed within a structured manifest and bound to a packet hash.
+The evidentiary packet includes, as applicable to the claims asserted in the submitted materials: high-resolution screenshots, page source and DOM captures, rendered text extracts, network activity records including requests, responses, headers, and timing data, console and runtime logs, interaction and navigation logs, and environment metadata including viewport, user agent, device profile, and execution settings. Each artifact is indexed within a structured manifest and bound to a cryptographically verified packet seal.

-Execution parameters are documented to permit independent technical review under equivalent controlled conditions. The resulting record reflects the live site state at the time of capture and is confined to the claims asserted in the submitted materials. The evidentiary packet is structured to withstand technical examination and adversarial scrutiny.
+Execution parameters are documented to permit independent technical review under equivalent controlled conditions. The resulting record reflects the live site state at the time of capture and is confined to the claims asserted in the submitted materials. The evidentiary packet is structured to withstand technical examination and adversarial scrutiny.
\ No newline at end of file
diff --git a/src/engine/intake/templates/ELIGIBLE_DESKTOP_MOBILE.md b/src/engine/intake/templates/ELIGIBLE_DESKTOP_MOBILE.md
index 43d77de..07e096d 100644
--- a/src/engine/intake/templates/ELIGIBLE_DESKTOP_MOBILE.md
+++ b/src/engine/intake/templates/ELIGIBLE_DESKTOP_MOBILE.md
@@ -1,4 +1,4 @@
-﻿ACCESS FORENSICS
+ACCESS FORENSICS
 INTAKE DETERMINATION
 MATTER ID: [AF-YYYY-NNNN]

@@ -8,12 +8,9 @@ Based on Access Forensics intake assessment of the complaint or demand materials

 This determination reflects feasibility under a bounded intake gate and does not quantify the number of conditions present or absent.

-BASELINE EXECUTION CONTEXTS:
-Desktop baseline is executed at 1366 x 900 CSS pixels at 100% browser zoom. Mobile baseline is executed at 393 x 852 CSS pixels at 100% browser zoom in portrait orientation. Browser version is recorded in the execution log. Reflow is evaluated separately per WCAG 1.4.10 using both direct and zoom-path methods.
-
 NEXT STEPS:
 Full engagement involves execution under defined desktop and mobile browser contexts pursuant to our controlled capture protocols. All capture artifacts are logged, time-stamped, and sealed within a structured evidentiary packet utilizing industry-standard cryptographic integrity verification (SHA-256) and documented chain-of-custody controls.

-The evidentiary packet includes, as applicable to the claims asserted in the submitted materials: high-resolution screenshots, page source and DOM captures, rendered text extracts, network activity records including requests, responses, headers, and timing data, console and runtime logs, interaction and navigation logs, and environment metadata including viewport, user agent, device profile, and execution settings. Each artifact is indexed within a structured manifest and bound to a packet hash.
+The evidentiary packet includes, as applicable to the claims asserted in the submitted materials: high-resolution screenshots, page source and DOM captures, rendered text extracts, network activity records including requests, responses, headers, and timing data, console and runtime logs, interaction and navigation logs, and environment metadata including viewport, user agent, device profile, and execution settings. Each artifact is indexed within a structured manifest and bound to a cryptographically verified packet seal.

-Execution parameters are documented to permit independent technical review under equivalent controlled conditions. The resulting record reflects the live site state at the time of capture and is confined to the claims asserted in the submitted materials. The evidentiary packet is structured to withstand technical examination and adversarial scrutiny.
+Execution parameters are documented to permit independent technical review under equivalent controlled conditions. The resulting record reflects the live site state at the time of capture and is confined to the claims asserted in the submitted materials. The evidentiary packet is structured to withstand technical examination and adversarial scrutiny.
\ No newline at end of file
diff --git a/src/engine/intake/templates/NOT_ELIGIBLE.md b/src/engine/intake/templates/NOT_ELIGIBLE.md
index 8a5ac9e..03b4b21 100644
--- a/src/engine/intake/templates/NOT_ELIGIBLE.md
+++ b/src/engine/intake/templates/NOT_ELIGIBLE.md
@@ -1,7 +1,7 @@
-﻿ACCESS FORENSICS
+ACCESS FORENSICS
 INTAKE DETERMINATION
 MATTER ID: [AF-YYYY-NNNN]

 DETERMINATION: NOT ELIGIBLE FOR FORENSIC EXECUTION

-Based on Access Forensics intake assessment of the complaint or demand materials provided, this matter does not satisfy intake criteria for controlled execution under our capture protocols as applied to the claims asserted in the submitted materials. No further testing will be performed.
+Based on Access Forensics intake assessment of the complaint or demand materials provided, this matter does not satisfy intake criteria for controlled execution under our capture protocols as applied to the claims asserted in the submitted materials. No further testing will be performed.
\ No newline at end of file
diff --git a/src/engine/intake/templates/NOT_ELIGIBLE_CONSTRAINTS.md b/src/engine/intake/templates/NOT_ELIGIBLE_CONSTRAINTS.md
index d6fc8bc..d6a67d9 100644
--- a/src/engine/intake/templates/NOT_ELIGIBLE_CONSTRAINTS.md
+++ b/src/engine/intake/templates/NOT_ELIGIBLE_CONSTRAINTS.md
@@ -1,4 +1,4 @@
-﻿ACCESS FORENSICS
+ACCESS FORENSICS
 INTAKE DETERMINATION
 MATTER ID: [AF-YYYY-NNNN]

@@ -8,12 +8,12 @@ Based on Access Forensics intake assessment of the complaint or demand materials

 If baseline access is intercepted by an automated challenge layer, access control, geofencing, or other technical barrier, Access Forensics does not attempt to defeat or bypass that barrier under allegation-scoped posture. The constraint is treated as a material feasibility event for reproducibility and is documented in the intake record.

-CONSTRAINT CLASS: [BOTMITIGATION / AUTHWALL / GEOBLOCK / HARDCRASH / NAVIMPEDIMENT]
-PLAIN-LANGUAGE BASIS: [One-line description of what was detected in the baseline lane]
+CONSTRAINT CLASS: {{CONSTRAINT_CLASS}}
+PLAIN-LANGUAGE BASIS: {{CONSTRAINT_BASIS}}

 A constraint artifact package documenting this feasibility event, including timestamped run artifacts, response header records, and a challenge fingerprint (SHA-256), is available as a case record.

 AUTHORIZED LANE OPTION:
 If counsel requires allegation-scoped verification of the alleged flow, access may be provided through an attorney-directed authorized lane such as a storefront password, preview link, or temporary allowlist window.

-If an authorized lane is subsequently provided by counsel, this determination will be superseded by a new intake determination reflecting the access conditions under which verification proceeded. The authorized lane and its source will be explicitly stated in the updated determination.
+If an authorized lane is subsequently provided by counsel, this determination will be superseded by a new intake determination reflecting the access conditions under which verification proceeded. The authorized lane and its source will be explicitly stated in the updated determination.
\ No newline at end of file
diff --git a/tests/template_resolution.test.js b/tests/template_resolution.test.js
new file mode 100644
index 0000000..996f0a1
--- /dev/null
+++ b/tests/template_resolution.test.js
@@ -0,0 +1,81 @@
+'use strict';
+
+const test = require('node:test');
+const assert = require('node:assert/strict');
+const { resolveTemplate } = require('../src/engine/intake/template_resolver.js');
+const { ENUMS } = require('../src/engine/intake/locked.js');
+
+test('Template Resolution: Ensure Enums exist before resolving', () => {
+  assert.ok(ENUMS.DETERMINATION_TEMPLATE.T1_DUAL, 'T1 enum missing');
+  assert.ok(ENUMS.DETERMINATION_TEMPLATE.T2_DESKTOP, 'T2 enum missing');
+  assert.ok(ENUMS.DETERMINATION_TEMPLATE.T4_NOT_ELIGIBLE, 'T4 enum missing');
+  assert.ok(ENUMS.DETERMINATION_TEMPLATE.T5_NOT_ELIGIBLE_CONSTRAINTS_BOT, 'T5 enum missing');
+  assert.ok(ENUMS.DETERMINATION_TEMPLATE.T6_NOT_ELIGIBLE_CONSTRAINTS_OTHER, 'T6 enum missing');
+});
+
+test('Template Resolution: T1_DUAL resolves to desktop/mobile eligible template', () => {
+  const result = resolveTemplate(ENUMS.DETERMINATION_TEMPLATE.T1_DUAL);
+  assert.ok(result.includes('ELIGIBLE FOR DESKTOP AND MOBILE TECHNICAL RECORD BUILD'), 'Missing expected T1 text');
+});
+
+test('Template Resolution: T2_DESKTOP resolves to desktop eligible template', () => {
+  const result = resolveTemplate(ENUMS.DETERMINATION_TEMPLATE.T2_DESKTOP);
+  assert.ok(result.includes('ELIGIBLE FOR DESKTOP TECHNICAL RECORD BUILD'), 'Missing expected T2 text');
+  assert.ok(!result.includes('AND MOBILE'), 'Should not include mobile');
+});
+
+test('Template Resolution: T4_NOT_ELIGIBLE resolves to not eligible template', () => {
+  const result = resolveTemplate(ENUMS.DETERMINATION_TEMPLATE.T4_NOT_ELIGIBLE);
+  assert.ok(result.includes('NOT ELIGIBLE FOR FORENSIC EXECUTION'), 'Missing expected T4 text');
+  assert.ok(!result.includes('CONSTRAINTS'), 'Should not include constraints logic');
+});
+
+test('Template Resolution: real T5 resolves to NOT_ELIGIBLE_CONSTRAINTS with BOTMITIGATION inserted', () => {
+  const result = resolveTemplate(ENUMS.DETERMINATION_TEMPLATE.T5_NOT_ELIGIBLE_CONSTRAINTS_BOT, 'BOTMITIGATION', 'bot basis');
+  assert.ok(result.includes('NOT ELIGIBLE FOR FORENSIC EXECUTION - CONSTRAINTS'), 'Missing expected T5 header');
+  assert.ok(result.includes('CONSTRAINT CLASS: BOTMITIGATION'), 'Missing constraint class injection');
+  assert.ok(result.includes('PLAIN-LANGUAGE BASIS: bot basis'), 'Missing constraint basis injection');
+});
+
+test('Template Resolution: real T6 resolves to NOT_ELIGIBLE_CONSTRAINTS with alternate class inserted', () => {
+  const result = resolveTemplate(ENUMS.DETERMINATION_TEMPLATE.T6_NOT_ELIGIBLE_CONSTRAINTS_OTHER, 'AUTHWALL', 'auth basis');
+  assert.ok(result.includes('NOT ELIGIBLE FOR FORENSIC EXECUTION - CONSTRAINTS'), 'Missing expected T6 header');
+  assert.ok(result.includes('CONSTRAINT CLASS: AUTHWALL'), 'Missing alternate constraint class injection');
+  assert.ok(result.includes('PLAIN-LANGUAGE BASIS: auth basis'), 'Missing constraint basis injection');
+});
+
+test('Template Resolution: T5 missing constraintClass hard-fails', () => {
+  assert.throws(
+    () => { resolveTemplate(ENUMS.DETERMINATION_TEMPLATE.T5_NOT_ELIGIBLE_CONSTRAINTS_BOT, null, 'some basis'); },
+    /constraintClass is required/,
+    'Must throw if constraintClass is missing'
+  );
+});
+
+test('Template Resolution: T6 missing constraintBasis hard-fails', () => {
+  assert.throws(
+    () => { resolveTemplate(ENUMS.DETERMINATION_TEMPLATE.T6_NOT_ELIGIBLE_CONSTRAINTS_OTHER, 'AUTHWALL', null); },
+    /constraintBasis is required/,
+    'Must throw if constraintBasis is missing'
+  );
+});
+
+test('Template Resolution: undefined category hard-fails', () => {
+  assert.throws(() => resolveTemplate(undefined), /category cannot be null or undefined/);
+});
+
+test('Template Resolution: null category hard-fails', () => {
+  assert.throws(() => resolveTemplate(null), /category cannot be null or undefined/);
+});
+
+test('Template Resolution: unknown category hard-fails', () => {
+  assert.throws(() => resolveTemplate('FAKE_CATEGORY'), /Unknown determination category/);
+});
+
+test('Template Resolution: T3_DESKTOP_MOBILE_CONSTRAINED hard-fails with required doctrine gap message', () => {
+  assert.throws(
+    () => { resolveTemplate(ENUMS.DETERMINATION_TEMPLATE.T3_DESKTOP_MOBILE_CONSTRAINED); },
+    /Missing canonical determination template for T3_DESKTOP_MOBILE_CONSTRAINED. AFIntakeTemplate.pdf provides no approved text for this state. Refusing to synthesize non-canonical determination content./,
+    'T3 must throw the exact required error message'
+  );
+});
\ No newline at end of file
--
2.53.0
