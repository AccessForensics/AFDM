# Golden Artifact Verification
Execution of `node src/engine/full_execution/run.js M-123 OPR-001` generated the valid packet `tmp/full_exec_out/M-123_packet`. The resulting verification outputs successfully hashed `manifest.json` using SHA-256 and sealed the value inside `03_Verification/packet_seal.txt`.
