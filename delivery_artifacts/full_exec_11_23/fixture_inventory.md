# Fixture Inventory
* `fixtures/full_execution/deterministic_valid.json`: Valid deterministic mock data used to generate a correct `M-101` full execution run and assert operator assignments and `ScopeDeltas`.
* `fixtures/full_execution/deterministic_fail.json`: Invalid mock data containing `independence_attestation: false` to prove fail-closed behaviors.
